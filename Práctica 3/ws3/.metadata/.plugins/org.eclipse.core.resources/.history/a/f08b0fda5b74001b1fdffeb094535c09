package uo.mp.s3.dome.model.item;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

import uo.mp.s3.dome.model.Item;

/**
 * Titulo: Clase ItemTest
 * 
 * @author Omitg
 * @version 18-02-2021
 */
public class ItemTest {
	
	/**
	 * Pruebas del constructor con parámetros de la clase Item:
	 * Prueba de funcionalidad:
	 * 1- Parametro Titulo valido -> Se crea el objeto
	 * 
	 * Prueba de robustez;
	 * 2- Parametro Titulo null -> Se lanza la excepción
	 */
	
	/**
	 * 
	 */
	@Test
	public void testConstructorValidParam() {
		Item item = new Item("Se7en (1995)");
		assertEquals("Se7en (1995)",item.getTitle());
	}
}
